# Views.
- Virtual tables that does not store any data on its own but presents a customized view of one or more tables in a database. 
- A view can be thought of as a pre-defined SELECT statement that retrieves data from one or more tables and returns a specific subset of data to the user. 
- so basically it is a logical table instead of a physical table. 

- Once a view is created, it can be used in the same way as a table in SQL queries, and any changes made to the underlying tables will be reflected in the view. 
- Simple Views - created from 1 single table. 
- Complex Views - Created from multiple tables with the help of joins, subquery etc. 

# Read Only Vs Updatable Views. 
-> Read-only views:
    - Aggregate 
-> Updatable views: 
    - Insert or Delete data in the underlying tables through the view. 

# Materialized Views:
- contains the results of a query.
- It is pre-computed and stored on disk which makes them much faster to access.

# Advantages of using a view. 
- No Physical storage. 
- security.
- make complex queries simple.

# User defined functions.
- Syntax
DELIMITER $$

CREATE FUNCTION Function_Name(
    Parameter_1 DataType, 
    Parameter_2 DataType,
    Parameter_n DataType,
)
RETURN Return_DataType
[NOT] DETERMINISTIC
BEGIN
    Function Body
    Return Return_Value
END $$

DELIMITER;


-----------
CREATE FUNCTION hello_world ()
RETURNS VARCHAR(255)
BEGIN

RETURN "Hello World";
END

SELECT hello_world() <-- calling a function. 

SELECT hello_world() AS message;

SELECT UPPER(name) FROM person


----
CREATE FUNCTION proper_name(
    name VARCHAR(255), 
    gender VARCHAR(255)
)
RETURN VARCHAR(255)
BEGIN
    DECLARE title VARCHAR(255)
    IF gender = 'M' THEN
        SET title = CONCAT('Mr', ' ', name);
    ELSE
        IF marries = 'Y' THEN
            SET title = CONCAT('Mrs', ' ', name);
        ELSE
            SET title = CONCAT('Ms', ' ', name);
        something
    END IF;
RETURN 1;
END